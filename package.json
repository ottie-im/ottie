{
  "name": "ottie",
  "version": "0.1.0",
  "private": true,
  "description": "🦦 Ottie — Agent-Native IM",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
