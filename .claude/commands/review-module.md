请以只读模式审查 `$ARGUMENTS` 模块：

1. 读取该模块的 `CLAUDE.md`
2. 读取该模块所有源代码文件
3. 对照 `/packages/ottie-contracts/src/index.ts` 检查接口是否匹配
4. 检查是否有违反 CLAUDE.md 规则的代码（比如引用了不该引用的模块）

**不要修改任何文件。** 只输出你的审查报告：
- 接口匹配情况
- 代码质量问题
- 违规引用
- 建议改进
