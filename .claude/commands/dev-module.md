请先读取以下文件了解项目上下文：

1. `/CLAUDE.md`（项目全局指南）
2. `/packages/ottie-contracts/src/index.ts`（接口合同，不可修改）
3. `$ARGUMENTS` 目录下的 `CLAUDE.md`（你的模块指南）

然后：
- 确认你理解了自己的职责和边界
- 列出你打算创建/修改的文件
- 等我确认后开始开发
- 每次修改后运行 `npx tsc --noEmit` 确认类型正确
